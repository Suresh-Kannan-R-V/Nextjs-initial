{
	"name": "@core/utils",
	"main": "src/index.ts",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"clean": "rm -rf .turbo && rm -rf dist && rm -rf node_modules",
		"typecheck": "tsc --noEmit"
	},
	"peerDependencies": {
		"dayjs": "^1.11.10"
	},
	"devDependencies": {
		"@core/typescript-config": "*",
		"typescript": "^5.2.2"
	}
}
