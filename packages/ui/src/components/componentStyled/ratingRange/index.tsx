'use client';
import { Button } from '@nextui-org/button';

interface ButtonProps {
	value?: string;
	width?: string;
}

export const ButtonStyled = ({ value, width }: ButtonProps) => {
	return <></>;
};
